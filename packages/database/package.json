{
  "name": "@react-router-gospel-stack/database",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "db:generate": "pnpm with-env prisma generate",
    "db:migrate:deploy": "pnpm with-env prisma migrate deploy",
    "db:migrate:dev": "pnpm with-env prisma migrate dev",
    "db:push": "pnpm with-env prisma db push",
    "db:seed": "pnpm with-env tsx src/seed.ts",
    "db:migrate:force": "pnpm with-env prisma migrate reset --force",
    "format": "pnpm with-env prisma format",
    "nuke": "rimraf .turbo node_modules src/generated",
    "clean": "rimraf .turbo src/generated",
    "lint": "eslint ./src",
    "studio": "pnpm with-env prisma studio",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@prisma/adapter-libsql": "catalog:prisma",
    "@prisma/client": "catalog:prisma"
  },
  "devDependencies": {
    "@react-router-gospel-stack/eslint-config": "workspace:*",
    "@react-router-gospel-stack/tsconfig": "workspace:*",
    "@types/node": "^24.9.1",
    "dotenv-cli": "^10.0.0",
    "prisma": "catalog:prisma",
    "rimraf": "catalog:",
    "tsup": "catalog:typescript",
    "tsx": "catalog:typescript",
    "typescript": "catalog:typescript"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
