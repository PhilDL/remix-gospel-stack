import type { UserRepository } from "@{{orgName}}/business/repositories";

import type { PrismaClient } from "../database/index.ts";
import { PrismaUserRepository } from "./user-repository.ts";

export { PrismaUserRepository } from "./user-repository.ts";

export const resolveRepositories = (
  db: PrismaClient,
): { user: UserRepository } => ({
  user: PrismaUserRepository(db),
});